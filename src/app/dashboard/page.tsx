import { DashboardClient } from "../DashboardClient";

export const metadata = {
  title: "Dashboard — Media Performance Observatory",
};

export default function DashboardPage() {
  return <DashboardClient />;
}
